# Development Log

このファイルでは、Olist Brazilian E-Commerce Public Datasetを用いた
AWSデータ分析ポートフォリオの開発過程を記録する。

---

## Project Overview

### Goal

AWS上にデータ分析環境を構築し、

S3 → Athena → Redshift → EC2 / Python

というデータ分析フローを実践する。

最終的には、EDA、統計分析、機械学習まで行い、
データからビジネス上の示唆を導くことを目標とする。

---

## 2026-09-19

### 1. S3環境構築

- Olistデータセットを使用することを決定
- S3バケットを作成
- RawデータをS3にアップロード
- CSVをテーブル単位のフォルダに整理

### 2. Athena環境構築

- Athenaを使用してS3上のCSVをSQLで分析する環境を構築
- Athenaのクエリ結果保存先をS3に設定
- `olist`データベースを作成


## 2026-09-20
### 3. ordersテーブル作成

- S3上の`olist_orders_dataset.csv`をAthenaの外部テーブルとして登録
- CSVのヘッダーを読み飛ばす設定を追加
- 動作確認を実施

### 4. customersテーブル作成

- S3上の`olist_customers_dataset.csv`をAthenaの外部テーブルとして登録
- `customers`テーブルの読み込みを確認

### 5. orders × customers JOIN

- `customer_id`をキーとしてordersとcustomersをJOIN
- 注文情報と顧客の地域情報を組み合わせて取得できることを確認

### 6. Timestamp型について

- ordersテーブルの日時列を`timestamp`型として定義した際にエラーが発生
- `string`型では正常に読み込み・JOINできることを確認
- 現時点では日時列を`string`として保持し、分析時にtimestampへ変換する方針とした

### 7. GitHub管理

- GitHubリポジトリを作成
- SourceTreeとGitHubを接続
- `sql/athena/`ディレクトリを作成
- Athenaで使用したSQLをGitHubで管理する方針とした

### 8. order_itemsテーブル作成

- S3上の`olist_order_items_dataset.csv`をAthenaの外部テーブルとして登録
- 注文ID、商品ID、販売者ID、商品価格、送料などの項目を取得できることを確認
- 今後の売上・商品・配送に関する分析に使用する予定
---

### 9.paymentsテーブル作成

- S3上の`olist_order_payments_dataset.csv`をAthenaの外部テーブルとして登録
- 注文ID、支払い方法、支払い回数、支払い金額などの項目を取得できることを確認
- 今後の支払い方法や注文金額に関する分析に使用する予定

### 10.データ型について

- `string`はIDや文字列として扱う項目に使用
- `double`は商品価格や送料など、小数を含む数値項目に使用
- 日時項目については、現時点では`string`として読み込み、分析時に必要に応じて`timestamp`へ変換する方針

### 11.reviewsテーブル作成

- S3上の`olist_order_reviews_dataset.csv`をAthenaの外部テーブルとして登録
- レビューID、注文ID、評価スコア、レビューコメント、レビュー日時などの項目を取得できることを確認
- 今後のレビュー評価と配送状況・商品・注文情報などの関係分析に使用する予定

### 12.int型について

- reviewsテーブルの`review_score`を`int`型として定義した際にエラーが発生
- `string`型では正常に読み込みできることを確認
- 現時点では`revies_socore`を`string`として保持し、分析時に`int`へ変換する方針とした

### 13. orders・order_items・reviewsのJOIN

- `order_id`をキーとして`orders`、`order_items`、`reviews`の3テーブルをJOIN
- 注文情報、商品価格、送料、レビュー評価を1つのデータセットとして取得
- 今後、商品価格・送料・配送状況とレビュー評価の関係を分析するための基礎データとして使用する

### 14. レビュー評価別の価格・送料集計

- `order_items`と`reviews`を`order_id`でJOIN
- レビュー評価（1～5）ごとにデータをグループ化（GROUP BY）
- 評価ごとの件数、平均商品価格、平均送料を集計（AVG）
- レビュー評価と商品価格・送料の関係を確認するための基礎集計を実施
- 今後、観察された傾向について統計的な検証を行う予定

### 15. 注文単位への集約とCTEの利用

- `order_items`は1注文に複数の商品が存在するため、JOIN後のデータ粒度に注意が必要であることを確認
- `order_id`ごとに商品価格と送料を集計し、注文単位のデータを作成
- `WITH`句を使用してCTE（Common Table Expression）を作成
- 注文単位の集計結果とレビュー評価をJOINし、注文ごとの分析用データを作成
- JOINによるデータ粒度の変化を確認し、分析単位を意識してSQLを設計する重要性を学習

## 2026.9.22
### 16. 15の続き

- `review_score`を確認すると、空欄、1-5以外の文字列が含まれていることを確認。
- その数を確認し、全体に対し数が少ないことを確認（60行）
- `order_id`が空、`order_id`がNULL、`review_score`が1-5以外を除外し、`orde_id`ごとの`review_score`を集計
- その集計クエリをCTEとして挿入し、10_create_order_summaryを10_order_level_summaryに変更。

### 17. 注文単位でのレビュー評価別集計

- 注文単位に集約した商品価格・送料とレビュー評価を使用し、レビュー評価別の平均注文金額・平均送料を集計
- 複数レビューが存在する注文については、レビュー評価の平均値を算出した後、四捨五入して1～5の5段階評価として集計
- レビュー評価別の平均注文金額・平均送料を比較し、評価と注文金額・送料の関係を確認
- 平均送料はレビュー評価が高くなるにつれて概ね低くなる傾向が確認された
- 平均注文金額は評価1から評価3にかけて低下した後、評価4・5ではやや上昇しており、単純な関係ではないことを確認
- 今回は記述統計による傾向の確認であり、因果関係や統計的有意性については今後検証する

### 18. Python.Rを用いて、データの記述統計量、分散分析を実施

- Athenaで出力したデータをPCにダウンロード
- Python、Rを用いて記述統計量を確認
- 箱ひげ図と記述統計を確認したところ、レビュー評価それぞれの価格と送料の平均値、中央値は低いものの、最大値が大きく、rangeが広いことを確認
- Kruskal-Wallis検定を実施したところ、レビュー評価に対する価格、送料はそれぞれ有意な変化を確認

## 2026.9.23
### 19.18の続き

実施した分析：
- 欠損値確認
- レビュー点数別の件数確認
- 全体およびレビュー点数別の記述統計
- 箱ひげ図による分布確認
- Kruskal-Wallis検定
- Dunn検定（Bonferroni補正）
- レビュー5点を基準とした平均・中央値の差の確認
- Kruskal-Wallis検定の効果量 ε²を算出

### 主な結果

#### Kruskal-Wallis検定

| 変数 | H統計量 | p値 |
|---|---:|---:|
| total_price | 299.1710 | 1.63538e-63 |
| total_freight | 932.2902 | 1.67964e-200 |

いずれもレビュー点数間で統計的な差が確認された。

#### Dunn検定

`total_price`：
- review_score 1・2と3～5の間で有意差が確認された
- 3・4・5の間では有意差は確認されなかった

`total_freight`：
- すべてのレビュー点数のペアで有意差が確認された

#### 効果量

| 変数 | ε² |
|---|---:|
| total_price | 0.003015 |
| total_freight | 0.009481 |

統計的には有意な差が確認された一方、効果量は小さかった。
サンプルサイズが大きいため、p値だけでなく効果量も確認する必要がある。

#### 考察
レビュー点数と注文価格・送料には統計的な関連が確認された。
特に送料はレビュー点数が高いグループほど平均値・中央値が低い傾向を示した。

ただし、本分析は観察データに基づくものであり、
送料や注文価格がレビュー低下の原因であるという因果関係までは示していない。

次の分析では、配送予定日と実配送日から配送遅延日数を作成し、
配送遅延とレビュー点数の関係を分析する。


### 20. 配送遅延とレビュー評価の関係を分析

#### 実施内容
- Athenaで実際の配達日と予定配達日の差を計算
- `delivery_delay_days = actual_delivery_date - estimated_delivery_date` と定義
- 配送遅延データとレビュー評価を `order_id` で結合
- `data/analysis/delivery_delay_review_summary.csv` を作成
- Positronで基本統計量を確認
- レビュー評価別の箱ひげ図を作成
- Kruskal-Wallis検定を実施
- Dunn検定（Bonferroni補正）を実施
- 効果量 ε² を算出

#### 結果
- 分析対象：95,830注文
- レビュー評価が高いほど配送遅延日数の中央値が小さくなる傾向を確認
- 中央値：
  - Review 1：-7日
  - Review 2：-10日
  - Review 3：-11日
  - Review 4：-12日
  - Review 5：-13日
- Kruskal-Wallis検定：
  - H = 3890.1397
  - p < 0.001
- Dunn検定（Bonferroni補正）：
  - 全てのレビュー評価群間で有意差
- 効果量：
  - ε² = 0.040555

#### 解釈
レビュー評価が低い注文ほど、予定配達日との差が遅延側に位置する傾向が確認された。
統計的には各レビュー評価群間で配送遅延日数に差が認められた。

#### 注意点
- 観察データによる分析であり、配送遅延がレビュー低下を引き起こしたという因果関係は確認できない。
- 配送遅延日数には大きな外れ値が存在する。
- 箱ひげ図では表示範囲を-50〜50日に制限している。
- レビュー評価1〜5でサンプル数に偏りがある。

### 21. 低評価予測モデルの構築

#### 実施内容
- 注文金額、送料、配送遅延日数を説明変数として、低評価（レビュー1～2点）を予測するロジスティック回帰モデルを構築
- 係数をオッズ比に変換し、各変数の影響方向を確認
- 予測結果について混同行列、Accuracy、Precision、Recall、F1-scoreを算出
- ROC-AUCとROC曲線を作成
- しきい値を0.50、0.40、0.30、0.20、0.10に変更して性能を比較
- Precision-Recall曲線を作成し、PrecisionとRecallのトレードオフを確認

#### 結果
レビュー1～2点を低評価（`low_review = 1`）と定義した。

データは、

- 通常評価：87.23%
- 低評価：12.77%

であった。

説明変数として、

- `total_price`
- `total_freight`
- `delivery_delay_days`

を使用した。

ロジスティック回帰の係数は、

- total_price：0.0084
- total_freight：0.2374
- delivery_delay_days：0.7747

となった。

標準化した説明変数に対するオッズ比は、

- total_price：約1.01
- total_freight：約1.27
- delivery_delay_days：約2.17

であった。

ただし、これらは観測データに基づくモデル上の関連を示すものであり、因果関係を示すものではない。

#### モデル評価

50%をしきい値とした場合、

- Accuracy：0.8786
- Precision：0.6981
- Recall：0.0878
- F1-score：0.1560
- ROC-AUC：0.6744

となった。

しきい値を変更したところ、今回試した範囲では0.20でF1-scoreが最大となった。

| Threshold | Precision | Recall | F1 |
|---:|---:|---:|---:|
| 0.50 | 0.6981 | 0.0878 | 0.1560 |
| 0.40 | 0.7105 | 0.1544 | 0.2537 |
| 0.30 | 0.6778 | 0.2663 | 0.3824 |
| 0.20 | 0.4176 | 0.3828 | 0.3994 |
| 0.10 | 0.1701 | 0.7006 | 0.2738 |

しきい値を下げることでRecallが上昇する一方、Precisionが低下するトレードオフを確認した。

#### 今回理解したこと

- ロジスティック回帰では、低評価になる確率を予測できる
- 係数を指数変換することでオッズ比として解釈できる
- Accuracyだけではクラス不均衡のある分類問題を十分に評価できない
- Precision、Recall、F1-scoreを目的に応じて使い分ける必要がある
- ROC-AUCは分類能力をしきい値全体で評価する指標である
- PR曲線ではPrecisionとRecallのトレードオフを確認できる
- しきい値はモデルそのものとは別に調整できる
- 実務ではTestデータを使ってしきい値を決定せず、Validationデータでしきい値を決定してからTestデータで最終評価する

## 2026-09-26
### 22. Train / Validation / Testによる最終評価
#### 実施内容
- データをTrain 60%、Validation 20%、Test 20%に分割し、`stratify`で低評価率を維持
- `StandardScaler`はTrainに対してfitし、ValidationとTestには同じ変換を適用
- Trainデータでロジスティック回帰を学習
- Validation上でしきい値候補を比較し、F1-scoreが最大となった0.24を選択
- しきい値を固定し、Testデータで最終評価

#### 分割結果
- Train：57,498件、低評価率12.77%
- Validation：19,166件、低評価率12.77%
- Test：19,166件、低評価率12.77%

#### Validationで選択したしきい値
- Threshold：0.24
- Precision：0.5794
- Recall：0.3337
- F1-score：0.4235

F1-scoreはPrecisionとRecallの調和平均であり、両者のグラフの交点とは限らない。しきい値ごとにPrecisionとRecallを計算し、F1-scoreが最大となる候補を選んだ。候補の探索は0.01刻みで行った。

#### Testでの最終結果

| 指標 | 結果 |
|---|---:|
| Accuracy | 0.8860 |
| Precision | 0.5919 |
| Recall | 0.3472 |
| F1-score | 0.4377 |
| ROC-AUC | 0.6838 |

混同行列（行が実際の評価、列が予測評価、順序は通常評価0・低評価1）：

|  | 予測：通常評価 | 予測：低評価 |
|---|---:|---:|
| 実際：通常評価 | 16,132 | 586 |
| 実際：低評価 | 1,598 | 850 |

Testの低評価2,448件のうち850件を検出し、1,598件を見逃した。低評価と予測した1,436件のうち850件が実際に低評価だった。クラス不均衡があるためAccuracyだけで評価せず、Precision・Recall・F1-scoreも併記する。

#### 解釈と限界
- しきい値0.24でのTest結果はValidationの結果と大きくは離れておらず、分割を分けた評価フローで性能を確認できた。
- ROC-AUCは0.6838で、ランダムより高い識別性能が見られたが、単独で実務適用を判断するには不十分。
- 低評価を見逃す割合は高く、誤検知・見逃しの業務コストを踏まえた運用判断は行っていない。
- 説明変数に配送遅延日数を含むため、注文後・配送完了後の情報を使うモデルであり、購入前の売上予測や介入にはそのまま使えない。
- このモデルは売上増加への直接的な効果を示すものではない。以降の分析では売上指標の基準値作成と改善候補の特定を優先する。

### 23. 月別・顧客州別の売上基準集計
#### 指標の定義
- 対象注文：`orders.order_status = 'delivered'`で、購入日・商品明細・顧客州が確認できる注文
- 集計粒度：購入月 × 顧客州
- GMV：注文に含まれる商品価格（`order_items.price`）の合計。送料は含めない
- 送料：`order_items.freight_value`の合計として別に集計
- 平均注文額：GMV ÷ 注文数
- 購入月は配送日ではなく`order_purchase_timestamp`を基準にした

#### 集計結果
- 期間：2016年9月～2018年8月（23か月。2016年11月は該当注文なし）
- 集計行数：556行（月・州の組み合わせ）
- 対象注文数：96,478件
- GMV合計：13,221,498.11（データ上の金額単位）
- 送料合計：2,198,275.64（GMVには含めず別集計）
- 全体の加重平均注文額：137.04（GMV合計 ÷ 注文数合計）
- SP州：40,501件、GMV 5,067,633.16で州別の注文数・GMVが最大。州別平均注文額は125.12
- 月別GMV最大：2017年11月の987,765.37（7,289件）
- 月・州の重複行、欠損値はなく、各行の平均注文額はGMV ÷ 注文数と一致した

#### 解釈と注意点
- SP州は取扱規模が最も大きい一方、平均注文額は全体平均を下回る。商品カテゴリや注文商品構成を追加で確認するための仮説候補とし、この差だけから施策や原因を断定しない。
- 2016年9月・10月・12月はそれぞれ1件、265件、1件と初期期間の件数が少ないため、通常月との比較や傾向判断に使う際は注意する。
- 注文状況`delivered`を条件にした今回の注文数96,478件は、配送遅延分析の対象数96,476件と2件異なる。配送遅延分析では実配送日と予定配送日の両方が必要だったのに対し、今回の売上集計では購入日を使うためである。必要日付の欠損による差か、データ結合条件の差かを後続のデータ品質確認で照合する。
- GMVは売上の代理指標であり、原価・販売手数料・広告費等がないため利益は算出できない。集計は観察データの記述であり、施策の因果効果を示さない。
- Athena結果CSVはローカルの`data/`に保存し、公開リポジトリには含めない。

## Next Steps
- 配送遅延分析の96,476件と今回の96,478件の差を、日付欠損・結合条件の観点で照合する
- 2016年の初期月と2018年8月の期間カバー状況を確認し、比較対象期間を決める
- 商品カテゴリ別に注文数・GMV・平均注文額を集計し、SP州などの大きな市場の構成を確認する
- その後、販売者やリピーターの分析を加え、改善仮説を絞る
